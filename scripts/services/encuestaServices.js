    /*

'use strict';

var baseUrl = 'http://nik.ws.tecnomcrm.com/nik/unidades';


nik.factory('UnidadesFactory', ['$resource', function ($resource) {
    return $resource(baseUrl, {}, {
        // método para obtener todas las unidades
        query: { method: 'GET', isArray: true },
        // método para insertar/modificar una unidad
        create: { method: 'PUT' }
    });
}]);


nik.factory('UnidadFactory', ['$resource', function ($resource) {
    return $resource(baseUrl + '/:id', {}, {
        // método para obtener una unidad
        query: { method: 'GET', isArray: false },
    });
}]);


*/
